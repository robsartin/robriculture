# Kaggriculture agent development
# Python 3.11 recommended (Kaggle's runtime; 3.10+ works locally)

# Core simulator + CLI. kaggle-environments is pinned to the version verified
# working with the kaggriculture env. Keep this matched to what Kaggle runs to
# avoid "works locally, errors on submit" surprises — bump it if the leaderboard
# runtime updates.
#
# 1.32.7 is what the ladder runs, read from `module_version` in all 63
# downloaded replays (#195). The previous 1.32.4 pin was stale, so CI was
# testing against a simulator the competition does not use — and the two
# disagree about outcomes, not just prices: #193's contender won 11/16 against
# the champion on 1.32.7 and 0/16 on 1.32.4.
kaggle-environments==1.32.7
kaggle>=1.6.0            # CLI, only needed for submitting/downloading

# Pulled in by kaggle-environments; pinned for reproducibility.
numpy==2.4.4

# Solver for harness/ceiling_lp.py (#136). Pinned like everything else so a
# drifted install cannot silently change an analysis result -- see #133.
scipy==1.18.0

# --- Dev / tuning tooling (optional but recommended) ---
pytest>=8.0             # local tournament harness + regression tests
pytest-cov>=5.0         # coverage measurement + CI gate (line>=85%, branch>=65%)
pytest-xdist>=3.5       # parallel test execution; the suite is dominated by
                        # independent full-game simulations (see ci.yml `-n auto`)
tqdm>=4.66              # progress bars for batch game runs
